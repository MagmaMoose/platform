# Copy to .dev.vars for local development (.dev.vars is gitignored).
# Long random string; the same value gates the admin REST API and the dashboard sign-in.
ADMIN_TOKEN=replace-with-a-long-random-string
