{
  "name": "@platform/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared build/lint/test config — single source of truth (replaces per-app copies).",
  "files": [
    "tsconfig.base.json",
    "tsconfig.worker.json",
    "vitest.base.ts",
    "prettier.config.mjs",
    "ruff.toml"
  ],
  "exports": {
    "./tsconfig.base.json": "./tsconfig.base.json",
    "./tsconfig.worker.json": "./tsconfig.worker.json",
    "./vitest": "./vitest.base.ts",
    "./prettier": "./prettier.config.mjs"
  }
}
